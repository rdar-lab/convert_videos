[pytest]
markers =
    docker: marks tests as Docker integration tests (run with -m docker)

# By default, exclude docker tests from test runs
addopts = -m "not docker"

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

