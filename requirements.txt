# Python dependencies
# Pinned to exact version for security and reproducibility
PyYAML==6.0.2

# Duplicate detection dependencies
ImageHash==4.3.1
Pillow==10.3.0

# System tools that must be installed:
# - ffmpeg (provides ffprobe)
# - HandBrakeCLI
